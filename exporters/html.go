package exporters

import (
	"errors"

	"github.com/senorprogrammer/gps-extractor/filetypes"
)

// ToHTML exports data in CSV format
func ToHTML(images []*filetypes.Image, outputFilePath string) error {
	return errors.New("FINISH ME")
}
